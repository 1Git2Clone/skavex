// Vite's ambient declarations: what makes `import './app.css'` and
// `import 'katex/dist/katex.min.css'` types rather than errors. Without it
// svelte-check treats every asset import as a missing module.
/// <reference types="vite/client" />
